import {Day} from "../../cl";

const day11 = new Day(
    (part, data: string[]) => {
        let answer = 0;
        return answer;
    },
    [null, null],
    [null, null]
).setPostTransform((transformed) => {
    return transformed;
});

(async () => {
    await day11.examples(1);
    //await day11.solve();
})();
